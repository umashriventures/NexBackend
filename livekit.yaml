# LiveKit Server Configuration
port: 7880
bind_addresses:
  - ""
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 60000
  use_external_ip: false

keys:
  # This matches the default keys we use in the backend for local dev
  devkey: secretkey

logging:
  level: info
